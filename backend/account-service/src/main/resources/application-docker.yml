spring:
  data:
    mongodb:
      host: ${MONGODB_HOST:mongodb}
      port: ${MONGODB_PORT:27017}
      uri: mongodb://${MONGODB_HOST:mongodb}:${MONGODB_PORT:27017}/banking_accounts

logging:
  level:
    com.banking: INFO
